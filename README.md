# Billboard

Pick an emotion image, write why you feel it, and post to a shared space.

## Run locally

1) Install:

```bash
npm install
```

2) Start:

```bash
npm run dev
```

3) Open:

- `http://localhost:3000`

## Notes

- Posts are saved in `posts.json`, so everyone connected to the same running server will see the same shared feed.